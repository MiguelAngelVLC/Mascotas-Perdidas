server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://root:oCLwRzeEqtJnSYOGsAAnJGXkBXtmrepU@mysql.railway.internal:3306/railway?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:mp_user}
    password: ${SPRING_DATASOURCE_PASSWORD:mp_password}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

app:
  jwt-secret: ${APP_JWT_SECRET:c2VjcmV0S2V5Rm9yTWFzY290YXNQZXJkaWRhc0p3dFNlY3JldEtleUZvck1hc2NvdGFzUGVyZGlkYXM=}
  jwt-expiration-ms: 86400000
  upload-dir: ${UPLOAD_DIR:./uploads}
  cors-origins: ${APP_CORS_ORIGINS:http://localhost:4200}

logging:
  level:
    com.mascotasperdidas: INFO
    org.springframework.security: WARN
